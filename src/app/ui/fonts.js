import { Barlow, Habibi } from 'next/font/google';

export const barlow = Barlow({ subsets: ['latin'], weight: ['400','500','600','700','800','900'], variable: '--font-barlow' });

export const habibi = Habibi({ subsets: ['latin'], weight: ['400'] });